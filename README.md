(This readme has to be changes and below details to moved to docs folder.)

## Before initializing git make sure to configure

- git config --global init.defaultBranch <name>
- git config --global user.name <name>
- git config --global user.email <email>
